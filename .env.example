CLAUDE_API_KEY=sk-ant-...
ALPACA_API_KEY=PK...
ALPACA_API_SECRET=...
FRONTEND_ORIGIN=http://localhost:3000

# Auth (MFA)
JWT_SECRET=change-me-to-a-long-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=   # python -c "from passlib.context import CryptContext; print(CryptContext(schemes=['bcrypt']).hash('yourpassword'))"
TOTP_SECRET=           # leave empty → call GET /api/auth/mfa/setup to generate, then paste the secret here
